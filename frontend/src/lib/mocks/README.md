# Mocks

JSON de prueba con la misma forma que la respuesta real del backend (FastAPI). Se usan mientras el backend no esté listo, para no bloquear el desarrollo visual. Se van agregando a medida que cada pantalla los necesita.
